# Email Configuration for Netlify Functions
# Copy this file to .env and fill in your actual values
# Then set these as environment variables in your Netlify dashboard

# Email service credentials (Gmail example)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Admin email for order notifications
ADMIN_EMAIL=admin@peptideplaza.com

# Optional: Custom SMTP settings
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false